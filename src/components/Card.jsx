import React from "react";

const Card = ({ data }) => {
  console.log(data);
  return (
    <div className="card">
      <div className="card-content">
        <h2>title</h2>
      </div>
    </div>
  );
};

export default Card;
